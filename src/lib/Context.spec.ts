import { Context } from "./Context";

describe("Context", () => {
  describe("constructor()", () => {
    it("should created member properties correctly", () => {
        
        const context = new Context({
        functionName,
        nodeModulesPath,
        startTime,
      });
    });
  });

  describe("done()", () => {});

  describe("fail()", () => {});

  describe("succeed()", () => {});

  describe("getRemainingTimeInMillis()", () => {});
});
